name: alson_education
description: تطبيق الألسن للعلوم الحديثة - نظام إدارة تعليمي متكامل
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_localizations:
    sdk: flutter

  # Database
  sqflite: ^2.2.8+4
  path: ^1.8.3

  # File Management
  file_picker: ^6.1.1
  open_file: ^3.3.3
  path_provider: ^2.1.1

  # Excel Handling
  excel: ^3.0.0

  # Networking & APIs
  http: ^0.13.6

  # UI & Helpers
  provider: ^6.1.1
  responsive_framework: ^0.2.0
  flutter_svg: ^2.0.7

  # Authentication & Security
  uuid: ^3.0.7
  shared_preferences: ^2.2.2

  # Communication
  url_launcher: ^6.1.11
  flutter_email_sender: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:

  assets:
    - assets/images/
    - assets/icons/
    - assets/documents/
